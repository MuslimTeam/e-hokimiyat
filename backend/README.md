# E-XOKIMIYAT Backend (minimal mock server)

Quick start:

1. From `d:/ehokimiyat/backend` install deps:

```bash
npm install
```

2. Run dev server:

```bash
npm run dev
```

Server runs on port `4000` by default and exposes endpoints under `/api` (e.g. `/api/tasks`).
